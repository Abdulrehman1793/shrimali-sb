package com.shrimali.model.enums;

public enum PrivacyLevel {
    PUBLIC,       // Everyone sees everything (Contact, Bio, Tree)
    PROTECTED,    // Everyone sees the Name/Photo/Tree, but Contact info is hidden
    PRIVATE       // Only Name and Photo are visible; all other data is hidden
}
